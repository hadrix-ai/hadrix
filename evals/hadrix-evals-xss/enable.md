# Enabling / disabling HTML rendering fixtures

Edit `hadrix.config.json`:

- `vulnerabilities.A03_injection.client_html_render`

This toggle covers all HTML rendering fixtures, including the NodeGoat and NodeVulnerable baselines.
