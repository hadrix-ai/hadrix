export const REPLAY_DESK_SOURCE = "replay-desk";

export const REPLAY_DESK_METADATA_FIELD = "replayDesk";

export const REPLAY_DESK_PAYLOAD_KEYS = {
  ticketId: "replayTicket",
  runId: "replayRunId",
  requestedBy: "replayActor"
} as const;
